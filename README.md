# portfolio

Jin Taizhen design portfolio website, implemented from the Figma portfolio pages.

## Run locally

```powershell
npm install
npm run dev
```

Then open `http://localhost:4173/`.

See `PORTFOLIO_HANDOFF.md` for the current implementation notes and `AGENTS.md` for the Figma preservation rules.
