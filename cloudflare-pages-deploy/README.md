# Jin Taizhen Portfolio

这是用于 GitHub 与 Cloudflare Pages 部署的静态网站文件。

## Cloudflare Pages 配置

- Build command: `exit 0`
- Build output directory: `/`
- 根目录必须直接包含 `index.html`
- 不需要运行 `server.mjs`

## GitHub 上传

将本文件夹中的全部内容上传到 GitHub 仓库根目录，保持 `public` 文件夹结构不变。
